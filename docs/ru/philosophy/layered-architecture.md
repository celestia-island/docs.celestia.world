# Слоистая архитектура

Экосистема остаётся управляемой, потому что она строго слоистая. Зависимости
направлены только в одну сторону: **нижестоящие сервисы потребляют
вышестоящую функциональность; общая функциональность никогда не реализуется
повторно.**

## Четыре слоя

| Слой | Проекты | Что они дают |
| --- | --- | --- |
| **Слой 0 — Аутентификация** | [kirino](https://github.com/celestia-island/kirino) | Примитивы нулевого доверия: подпись и обновление JWT, хеширование паролей Argon2id, ограничение частоты входа, движок RBAC, хранилище приглашений, сессии |
| **Слой 1 — Платформа** | [plana](https://github.com/celestia-island/plana) | Общие средства: типы и маршрутизация JSON-RPC 2.0, DTO сервисов, определение сети, SSE-сессии, предохранители, учёт и тарификация LLM |
| **Слой 2 — UI** | [hikari](https://github.com/celestia-island/hikari) | Библиотека UI-компонентов (Vue/TS + Rust), общая для всех webUI |
| **Слой 3 — Сервисы** | [arona](https://github.com/celestia-island/arona), [shittim-chest](https://github.com/celestia-island/shittim-chest), [entelecheia](https://github.com/celestia-island/entelecheia), [evernight](https://github.com/celestia-island/evernight), [malkuth](https://github.com/celestia-island/malkuth), [lagrange](https://github.com/celestia-island/lagrange) | Только бизнес-логика. Они потребляют слои 0–2 и добавляют поведение, делающее каждый продукт реальным |

## Доктрина

1. **Никогда не реализовывать дважды.** Перед написанием кода спросите: есть
   ли это в kirino? есть ли это в plana? есть ли это в hikari? Пример: типы
   JSON-RPC приходят из plana, JWT — из kirino, ограничение частоты входа — из
   kirino, предохранители — из plana, DTO здоровья — из plana, тарификация —
   из plana.
2. **Общая функциональность идёт наверх.** Функция, которую будут
   использовать два и более сервисов, сначала встраивается в kirino, plana
   или hikari, а затем потребляется.
3. **Никаких обратных зависимостей.** Сервисы зависят от kirino/plana/hikari;
   plana может зависеть от kirino; kirino никогда не зависит от plana или
   hikari.
4. **Сначала расширяйте вышестоящее, потом потребляйте.** Если вышестоящему
   слою не хватает нужной функциональности, расширяйте его, затем потребляйте.
   Новая функциональность никогда не прототипируется в сервисе, чтобы потом
   быть переписанной.
5. **Межрепозиторные зависимости — это git-ссылки.** Все репозитории
   потребляют вышестоящие через git-ссылки на ветку `master` (или
   зафиксированные версии), никогда не через локальные пути. Каждый
   репозиторий собирается одинаково на любой машине.

## Почему это важно

- **Один фикс распространяется.** Исправление безопасности в kirino достигает
  каждого сервиса с помощью обновления зависимости, а не поисков по
  переписанным реализациям.
- **Ревью пропорционально риску.** Изменения слоя 3 — это логика продукта;
  изменения слоя 0 — инфраструктура; барьер ревью это отражает.
- **Карта остаётся читаемой.** Новые инженеры читают эту страницу и знают, где
  живёт любая функциональность. [Карта проектов](../ecosystem/projects.md) —
  полный перечень.

## Подробнее

- [Почему celestia-island](./why.md) — проблема, стоящая за слоями.
- [Принципы безопасности](./safety.md) — доктрина, лежащая поверх слоёв.
- [Карта проектов](../ecosystem/projects.md) — каждый репозиторий по слоям.
